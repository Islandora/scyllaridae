# GitHub Actions OIDC configuration for integration tests (forwardAuth: false)
jwksUri: "https://token.actions.githubusercontent.com/.well-known/jwks"
forwardAuth: false

allowedMimeTypes:
  - "*"
cmdByMimeType:
  default:
    cmd: "/app/cmd.sh"
