# GitHub Actions OIDC configuration for integration tests
jwksUri: "https://token.actions.githubusercontent.com/.well-known/jwks"

allowedMimeTypes:
  - "*"
cmdByMimeType:
  default:
    cmd: "/app/cmd.sh"
