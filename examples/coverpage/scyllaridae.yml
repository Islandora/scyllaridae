queueName: islandora-pdf-coverpage
allowedMimeTypes:
  - "application/pdf"
cmdByMimeType:
  default:
    cmd: "/app/cmd.sh"
    args:
      - "%canonical"
      - "%source-uri"
      - "%file-upload-uri"
      - "%destination-uri"
