allowedMimeTypes:
  - "text/plain"
  - "text/markdown"
cmdByMimeType:
  default:
    cmd: /usr/local/bin/pandoc
    args:
      - "-f"
      - "%source-mime-ext"
      - "-t"
      - "%destination-mime-ext"
