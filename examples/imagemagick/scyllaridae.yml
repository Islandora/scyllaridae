allowedMimeTypes:
  - "application/pdf"
  - "image/*"
cmdByMimeType:
  "application/pdf":
    cmd: magick
    args:
      - "pdf:-[0]"
      - "%args"
      - "%destination-mime-ext:-"
    cmd: magick
  "image/tiff":
    args:
      - "-"
      - "%args"
      - "-define tiff:subfile=0"
      - "%destination-mime-ext:-"
  default:
    cmd: magick
    args:
      - "-"
      - "%args"
      - "%destination-mime-ext:-"
